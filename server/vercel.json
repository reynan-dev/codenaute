{
	"version": 2,
	"builds": [
		{
			"src": "src/index.ts",
			"use": "@vercel/node",
			"env": {
				"NODE_ENV": "production",
				"NODE_PATH": "dist/"
			}
		}
	],
	"routes": [
		{
			"src": "(.*)",
			"dest": "src/index.ts"
		}
	]
}
