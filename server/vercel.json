{
	"version": 2,
	"builds": [
		{
			"src": "src/index.ts",
			"use": "@vercel/node",
			"config": {
				"includeFiles": ["src/**/*", "dist/**/*"],
				"command": "npm run build && npm run start"
			}
		}
	],
	"routes": [
		{
			"src": "(.*)",
			"dest": "src/index.ts"
		}
	]
}
