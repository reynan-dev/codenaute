{
	"version": 2,
	"env": {
		"NODE_ENV": "production",
		"PORT": 3000,
		"NODE_PATH": "dist/"
	},
	"builds": [
		{
			"src": "src/index.ts",
			"use": "@vercel/node",
			"config": { "includeFiles": ["dist/**"] }
		}
	],
	"routes": [
		{
			"src": "(.*)",
			"dest": "src/index.ts"
		}
	]
}
