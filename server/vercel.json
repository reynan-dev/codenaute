{
	"version": 2,
	"buildCommand": "npm run build",
	"outputDirectory": "dist",
	"devCommand": "npm run build",
	"functions": {
		"src/index.ts": {
			"includeFiles": "src/**/*"
		}
	},
	"routes": [
		{
			"src": "(.*)",
			"dest": "src/index.ts"
		}
	]
}
